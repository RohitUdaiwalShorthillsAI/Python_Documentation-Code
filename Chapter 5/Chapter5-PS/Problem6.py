s = {8, 7, 12, "Harry", [1,2]}
print(s)

# You cannot change the values inside a list contained in a set in Python, In fact, you cannot even have a list as an element is a set because sets in Python require all their elements to be immutablle and hasable. Lists are mutable and not hashable, so they cannot be added to a set.

